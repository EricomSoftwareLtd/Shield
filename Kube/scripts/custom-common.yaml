---
shield-common:  
  enabled: true

common:
  doNotDelete: true

# When ELK is running on a different system (e.g. Split Mode), uncomment these
# lines and replace the IP (enter the IP of the server running ELK)
# fluent-bit-out-syslog:
#  elasticHost: "10.10.0.1:9200"