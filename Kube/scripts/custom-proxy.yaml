---
shieldproxy:
  enabled: true
consul:
  enabled: false
  server:
    enabled: no
  client:
    join: 
      - management-consul-server.management.svc.cluster.local
    ports:
      httPort: 8501
      dnsPort: 8601
      grpcPort: 8503
